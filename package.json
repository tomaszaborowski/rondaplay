{
  "name": "rondaplay-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "npm run start --workspace=apps/web",
    "lint": "turbo lint"
  },
  "packageManager": "npm@11.11.0",
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
